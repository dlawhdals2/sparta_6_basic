### [사용된 데이터 소스 설명] 출처, 구성, 관측단위, 주요 변수를 소개하세요.

## 데이터 출처
- 설명: 본 데이터는 Kaggle의 H&M Personalized Fashion Data에서 제공된 공개 데이터셋이다.
- 예시: 글로벌 SPA 브랜드 기업인 H&M의 고객, 상품, 거래 로그를 포함한다.

## 데이터셋 구성
- 설명: 고객(customers_hm), 거래(transactions_hm), 상품(articles_hm) 총 3개 CSV 파일로 구성되어 있다.
- 예시: 본 프로젝트에서는 2개 혹은 3개의 테이블을 머지하여 분석용 데이터셋을 구축하였다.

## 관측 단위와 기간
- 설명: 관측 단위는 고객 - 거래 - 상품 조합이며, 개별 구매 기록을 의미한다.
- 예시: 하나의 행(row)은 특정 고객이 특정 날짜에 특정 상품을 구매한 정보를 담는다.
프로젝트에 사용된 데이터는 2019년 한 해만을 다룬 데이터이므로, 관측 기간 역시 2019년으로 한정한다.

## 주요 변수 소개
- 설명: 과제 수행에 필요한 핵심 변수들을 소개한다.
- 예시: 고객(age), 거래(t_dat, price, sales_channel_id), 상품(prod_name, product_type_name)



### [EDA] 분석할 데이터의 행/열 개수를 제시
## 전처리 전
customers.shape, transactions.shape, articles.shape 
((1048575, 6), (1048575, 5), (105542, 25))



### [EDA] 분석할 데이터의 컬럼 타입과 기술통계(min/median/mean/max, 결측치 수)를 제시



### [전처리 과정] 결측치/이상치/전처리 처리 규칙을 수립·실행, 처리 이유

## 전처리 후
customers.shape, transactions.shape, articles.shape
((1048575, 7), (1048575, 9), (104857, 25))

## 전처리 내용
- transactions 테이블에 "t_dat" 컬럼의 파생 변수(년, 월, 요일) 생성에 의한 칼럼 수 변화 
- customers 테이블에 "age" 컬럼의 파생 변수(연령대) 생성에 의한 칼럼 수 변화
- transactions 테이블에 "sales_channel_id" 컬럼의 파생 변수(맵핑) 생성에 의한 칼럼 수 변화
- articles 테이블에 "product_group_name" 컬럼 중 상품 그룹이 50개 미만인 경우, 희소 카테고리로 판단하여 "Others"로 대체
- customers 테이블에 "club_member_status", "fashion_news_frequency" 컬럼의 경우, value값이 각각 3개씩 있으므로, 분석의 용이성을 위해 category 타입으로 형 변환
- customers 테이블에 "FN", "Active" 컬럼의 경우, value값이 각각 2개 (0,1)로 이루어져 있으므로, 분석의 용이성을 위해 불리언 인덱싱 실행

## 결측치/이상치 처리 내용
- customers 테이블에 "fashion_news_frequency" 컬럼의 결측치를 "NONE"로 대체
- articles 테이블에 "perceived_colour_master_id" 컬럼의 이상치 처리로 인한 행 수 변화
- articles 테이블에 "detail_desc" 컬럼의 결측치를 "No Description"로 대체